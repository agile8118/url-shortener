version: 0.2

phases:
  build:
    commands:
      - cd ./client-js && npm install && cd .. && cd ./sass && npm install && cd ..
      - npm run build

artifacts:
  files:
    - public/*
    - scripts/*
    - server/*
    - appspec.yml
    - package-lock.json
    - package.json
